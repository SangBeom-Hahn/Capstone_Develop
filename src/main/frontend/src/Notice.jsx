          import React from 'react';
          import Header from './Header'; // Header 컴포넌트를 import

          const Notice = () => {
            return (
              <div>
                <Header /> {/* Header 컴포넌트 사용 */}
                {/* 이후 Notice 컴포넌트의 내용 추가 */}
              </div>
            );
          };

          export default Notice;
